# Nothing Special

Just some utils...

example usage:
```javascript   
import { build } from '@awesomeness-js/utils';

const built = build({
    src: './src',
    dest: ['./', 'index.js']
});

console.log({ built });

```